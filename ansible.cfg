[defaults]
inventory = inventories/workstations
roles_path = roles
host_key_checking = False
display_skipped_hosts = False
callbacks_enabled = profile_tasks
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
