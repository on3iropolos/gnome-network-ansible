# Copy to .env and set your passwords
# DO NOT commit .env to version control

SSH_PASSWORD=your_ssh_password_here
ENCRYPTION_PASSWORD=your_encryption_password_here
USER_PASSWORD=your_user_password_here
USER_SSH_KEY="ssh-ed25519 AAAAC3..."

# SSH public key mounted from: ${HOME}/.ssh/git_on3iropolos_ed25519.pub
# Update docker-compose.yml and host_vars if using a different key