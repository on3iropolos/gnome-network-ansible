diff --git a/Dockerfile b/Dockerfile
index bf7311b..137497f 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,49 +1,21 @@
-# Use the official Alpine Linux base image
-FROM alpine:latest
+FROM alpine/ansible:latest
 
-ARG GLIBC_VERSION=2.35-r1
-ARG GLIBC_ARCH=x86_64
+# Install additional tools for molecule and linting
+RUN apk --no-cache add \
+    bash \
+    git \
+    openssh-client \
+    docker-cli \
+    py3-pip
 
-RUN set -e \
-    && apk --no-cache add ca-certificates wget \
-    && wget -q -O /etc/apk/keys/sgerrand.rsa.pub https://alpine-pkgs.sgerrand.com/sgerrand.rsa.pub \
-    && wget https://github.com/sgerrand/alpine-pkg-glibc/releases/download/${GLIBC_VERSION}/glibc-${GLIBC_VERSION}.apk \
-    && apk add --no-cache glibc-${GLIBC_VERSION}.apk \
-    && rm glibc-${GLIBC_VERSION}.apk
+# Install Molecule, ansible-lint and plugins
+RUN pip3 install --no-cache-dir --break-system-packages \
+    molecule \
+    molecule-plugins[docker] \
+    docker \
+    ansible-lint
 
-VOLUME ["/root"]
-VOLUME ["/data"]
 WORKDIR /data
 
-# Update and Install Dependencies
-RUN set -e \
-    && apk --no-cache update \
-    && apk --no-cache add \
-        bash \
-        fish \
-        binutils \
-        bind-tools \
-        curl \
-        jq \
-        groff \
-        less \
-        openssl \
-        unzip \
-        git \
-        openssh-client \
-        sshpass \
-        rsync \
-        py3-pip \
-        py3-dnspython \
-        py3-jmespath \
-        ansible \
-        ansible-lint \
-        docker-cli \
-    && rm -rf /var/cache/apk/* /tmp/* \
-    && pip3 install --no-cache-dir --break-system-packages \
-        molecule \
-        molecule-plugins[docker] \
-        docker
-
 # Default command to bash shell
 CMD ["/bin/bash"]
diff --git a/Makefile b/Makefile
deleted file mode 100644
index 95cf36d..0000000
--- a/Makefile
+++ /dev/null
@@ -1,12 +0,0 @@
-.PHONY: lint help setup
-
-ANSIBLE_PLAYBOOKS := install.yml provision.yml
-
-help: ## Show this help message
-	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | sort | awk 'BEGIN {FS = ":.*?## "}; {printf "\033[36m%-20s\033[0m %s\n", $$1, $$2}'
-
-setup: ## Install pre-commit hooks
-	pre-commit install
-
-lint: ## Run ansible-lint on all playbooks and roles
-	ansible-lint $(ANSIBLE_PLAYBOOKS) roles/
diff --git a/README.md b/README.md
index 8a91f81..09f7efc 100644
--- a/README.md
+++ b/README.md
@@ -5,6 +5,7 @@ Automated configuration for Arch Linux workstations.
 ## Requirements
 
 - Ansible: `pacman -S ansible` or `pip install ansible`
+- [xc](https://xc.sh/) - Task runner (install via AUR: `paru -S xc-bin`)
 - Access to target machine (local or SSH)
 
 ## Setup
@@ -56,9 +57,94 @@ For Kubernetes demonstrations and portfolio showcase, see:
 ## Development
 
 ```bash
-make setup      # Install deps and pre-commit hooks
-make lint      # Run ansible-lint
-cd roles/<role> && molecule test  # Test a role
+# Install pre-commit hooks
+xc setup
+
+# Run linter
+xc lint
+
+# Build development Docker image
+xc docker-build
+
+# Run interactive dev shell in Docker
+xc docker-dev
+
+# Test a role with Molecule
+cd roles/<role> && molecule test
+
+# Or use xc for Molecule (specify role name)
+xc molecule-test <role-name>
+```
+
+Open `xc` without arguments for interactive task picker.
+
+## Tasks
+
+### setup
+
+Install pre-commit hooks.
+
+```bash
+docker run --rm -v "$(pwd):/data" -v ~/.gitconfig:/root/.gitconfig -w /data gnome-network-ansible pre-commit install
+```
+
+### lint
+
+Run ansible-lint on all playbooks and roles.
+
+```bash
+docker run --rm -v "$(pwd):/data" -w /data gnome-network-ansible ansible-lint install.yml provision.yml roles/ roles/*/tasks/*.yml
+```
+
+### docker-build
+
+Build the development Docker image.
+
+```bash
+docker build -t gnome-network-ansible .
+```
+
+### docker-dev
+
+Run an interactive development shell in Docker.
+
+```bash
+docker run --rm -v "$(pwd):/data" -v /var/run/docker.sock:/var/run/docker.sock -v ~/.ssh:/root/.ssh:ro -w /data gnome-network-ansible
+```
+
+### k3d-create
+
+Create local k3d cluster for Kubernetes development.
+
+```bash
+docker run --rm -v "$(pwd):/data" -v /var/run/docker.sock:/var/run/docker.sock -w /data gnome-network-ansible ansible-playbook k8s.yml
+```
+
+### molecule-test
+
+Run Molecule tests for a specific role.
+Usage: xc molecule-test <role-name>
+Example: xc molecule-test network
+
+```bash
+docker run --rm -v "$(pwd):/data" -v /var/run/docker.sock:/var/run/docker.sock -w /data gnome-network-ansible bash -c "cd roles/$1 && molecule test"
+```
+
+### provision
+
+Run Ansible provisioning playbook.
+Usage: xc provision [check] - pass "check" as argument for dry-run
+
+```bash
+docker run --rm --network=host -v "$(pwd):/data" -v /var/run/docker.sock:/var/run/docker.sock --entrypoint /bin/bash gnome-network-ansible -c "cp -r ~/.ssh/id_rsa ~/.ssh/id_rsa.pub /tmp/.ssh/ && chmod 600 /tmp/.ssh/* && ansible-galaxy collection install -r requirements.yml && ANSIBLE_PRIVATE_KEY_FILE=/tmp/.ssh/id_rsa ansible-playbook provision.yml ${1:+--$1}"
+```
+
+### install
+
+Run Ansible installation playbook (for fresh Arch installs).
+
+```bash
+docker run --rm -v "$(pwd):/data" -v /var/run/docker.sock:/var/run/docker.sock -v ~/.ssh:/root/.ssh:ro -w /data gnome-network-ansible bash -c "ansible-galaxy collection install -r requirements.yml && ansible-playbook install.yml"
 ```
 
 ## Issue Tracking with Beads (bd)
diff --git a/docker-compose.yml b/docker-compose.yml
index 634f4a2..68735bf 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -3,18 +3,13 @@ services:
     build:
       context: .
       dockerfile: Dockerfile
-      args:
-        GLIBC_ARCH: x86_64 # or aarch64 for ARM64 builds
     volumes:
       - .:/data
-      - ${HOME}/.ssh/github_ssh_key.pub:/root/.ssh/user_public_key.pub:ro
+      - ${HOME}/.ssh:/root/.ssh:ro
       - ~/.gitconfig:/root/.gitconfig:ro
       - /var/run/docker.sock:/var/run/docker.sock
-      - /var/run/libvirt/libvirt-sock:/var/run/libvirt/libvirt-sock:rw
     stdin_open: true
     tty: true
-    group_add:
-      - "128" # Host libvirt group GID
     environment:
       ANSIBLE_CONFIG: /data/ansible.cfg
       SSH_PASSWORD: "${SSH_PASSWORD}"
diff --git a/inventories/inventory.yml b/inventories/inventory.yml
index c528eb2..b245157 100644
--- a/inventories/inventory.yml
+++ b/inventories/inventory.yml
@@ -5,4 +5,5 @@ all:
         # whimsyforge.gnome.network:
         #   ansible_connection: local
         stump.gnome.network:
-          ansible_connection: local
\ No newline at end of file
+          # Provision via SSH from container to host
+          ansible_user: "{{ lookup('env', 'USER') | default('root', true) }}"
\ No newline at end of file
diff --git a/roles/docker/tasks/main.yml b/roles/docker/tasks/main.yml
index 7c4ef76..c108530 100644
--- a/roles/docker/tasks/main.yml
+++ b/roles/docker/tasks/main.yml
@@ -6,6 +6,13 @@
     state: present
   tags: [docker]
 
+- name: Create Docker config directory
+  ansible.builtin.file:
+    path: /etc/docker
+    state: directory
+    mode: "0755"
+  tags: [docker]
+
 - name: Configure Docker daemon address pool
   ansible.builtin.copy:
     dest: /etc/docker/daemon.json
@@ -19,11 +26,16 @@
   notify: Restart Docker
   tags: [docker]
 
+- name: Check if Docker service is enabled and running
+  ansible.builtin.command: /usr/bin/systemctl is-enabled docker
+  register: docker_enabled
+  changed_when: false
+  failed_when: false
+  tags: [docker]
+
 - name: Enable and start Docker service
-  ansible.builtin.systemd:
-    name: docker
-    enabled: true
-    state: started
+  ansible.builtin.command: /usr/bin/systemctl enable --now docker
+  when: docker_enabled.rc != 0
   tags: [docker]
 
 - name: Get user groups
