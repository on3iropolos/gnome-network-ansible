# Staged Changes Summary
**Date:** 2026-04-28 23:18:19
**Patch File:** `.agent/staged_changes_20260428_231819.patch`

## Overview
This changeset modernizes the Docker setup, replaces Make with xc task runner, and improves Docker role reliability.

## Files Changed (6 files, +122 -68)

### 1. Dockerfile
**Simplified Docker image setup**
- Replaced custom Alpine build with `alpine/ansible:latest` base image
- Removed manual glibc installation (no longer needed)
- Removed fish shell, bind-tools, and other unused packages
- Streamlined to install only: bash, git, openssh-client, docker-cli, py3-pip
- Moved molecule/ansible-lint installation to pip
- Removed unused VOLUME directives

### 2. Makefile (DELETED)
- Removed in favor of xc task runner
- Tasks moved to xc-compatible README documentation

### 3. README.md
**Major update - replaced Make with xc**
- Added xc task runner requirement
- Documented all tasks: setup, lint, docker-build, docker-dev, k3d-create, molecule-test, provision, install
- Added detailed task descriptions with Docker commands
- Each task shows the exact Docker command for reproducibility

### 4. docker-compose.yml
**Simplified compose configuration**
- Removed GLIBC_ARCH build arg (no longer needed)
- Changed SSH mount to entire `.ssh` directory (was only public key)
- Removed libvirt socket mount and group_add (libvirt no longer needed in container)
- Kept docker socket mount for Molecule testing

### 5. inventories/inventory.yml
**Updated inventory configuration**
- Commented out `ansible_connection: local` for stump.gnome.network
- Added dynamic user lookup: `ansible_user: "{{ lookup('env', 'USER') | default('root', true) }}"`
- Enables SSH provisioning from container to host

### 6. roles/docker/tasks/main.yml
**Improved Docker service management**
- Added task to create `/etc/docker` directory before writing daemon.json
- Added pre-check: `systemctl is-enabled docker` to detect if already enabled
- Changed from systemd module to `systemctl enable --now docker` command
- Only enables Docker if not already enabled (idempotency fix)

## How to Reapply These Changes
```bash
# From project root
git apply .agent/staged_changes_20260428_231819.patch
```

## Key Improvements
1. **Smaller Docker image** - Removed unnecessary packages and custom glibc build
2. **Better task runner** - xc provides interactive task picker, documented in README
3. **Fixed Docker role** - Service enable is now idempotent
4. **SSH flexibility** - Inventory can provision via SSH with dynamic user detection
5. **Cleaner compose** - Removed libvirt dependencies from container

## Notes
- Requires xc task runner: `paru -S xc-bin` (AUR) or see https://xc.sh/
- Docker image now uses official ansible image as base
- SSH keys mounted to container for provisioning scenarios
