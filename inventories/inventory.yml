all:
  children:
    workstations:
      hosts:
        # whimsyforge.gnome.network:
        #   ansible_connection: local
        stump.gnome.network:
          # Provision via SSH from container to host (localhost)
          # ansible_connection: local  # Removed: uses SSH by default
          ansible_user: "{{ lookup('env', 'USER') | default('root', true) }}"
